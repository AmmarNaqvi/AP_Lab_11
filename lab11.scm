;Adanced Programming Lab 11
;Syed Ammar Abbas Naqvi
;Reg # 6046


(define (if m)
  (f (m m)) (f (m m)))

(define (loop m)
  ((isZero m)0 add f(m-1) m))

(define (aCompletelyUselessFunctionCreatedOnlyToMakeYourLifeMiserable m n)
(or (pred m n) (isZero m) (if loop n) (add m n)))